name: "base"
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - pip:
    - streamlit
    - jupyter-server-proxy
    - matplotlib
    - seaborn
    - plotly
    - bokeh
    - pandas
    - altair
prefix: "/opt/conda"
